

// Функция возвращает ставку акцизного налога 
//
Функция ПолучитьСтавкуАкцизногоНалога(СтавкаНДС) Экспорт

	Если ЗначениеЗаполнено(СтавкаНДС) И СтавкаНДС.СоставнойНалог Тогда
		Возврат 0.05;
	Иначе
		Возврат 0;	
	КонецЕсли; 
	

КонецФункции // ПолучитьСтавкуАкцизногоНалога()
