
&НаСервере
Процедура ЗагрузитьFTPНаСервере()
	МассивФТП = Новый Массив;
	
	
	ТекСтрока = Элементы.Список.ТекущаяСтрока;
	
	ПараметрыУчЗаписи = Справочники.ТК_УчетныеЗаписиEDI.ПолучитьПараметрыПодключения(ТекСтрока,ЛОжь);
	
	МассивФТП.Добавить(Новый Структура("Учетка,Подразделение,Параметры",ТекСтрока,Неопределено,ПараметрыУчЗаписи));

	ТК_ОбменEDI.ЗагрузкаFTPСобщенийEDI(МассивФТП);	

КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьFTP(Команда)
	ЗагрузитьFTPНаСервере();
КонецПроцедуры
