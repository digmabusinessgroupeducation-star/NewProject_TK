
Процедура ПриЗаписи(Отказ, Замещение)
	
	Если Отказ Тогда 
		Возврат;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	МассивУзлов = ТК_ОбменCentrПовтИсп.МассивУзловЗУП();
	Если МассивУзлов.Количество() > 0 Тогда 
		Для Каждого Стр Из ЭтотОбъект.Отбор Цикл 
			Если Стр.Использование И Стр.Имя = "ТорговаяПлощадка" И ЗначениеЗаполнено(Стр.Значение) Тогда 
				ПланыОбмена.ЗарегистрироватьИзменения(МассивУзлов, Стр.Значение);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
		
КонецПроцедуры
